# SpinDeck ESP32 firmware

This folder contains the Arduino firmware for the SpinDeck controller. The firmware handles the TFT display, rotary encoder, push button and USB serial protocol used by the Windows application.

Open `SpinDeck_ESP32.ino` in Arduino IDE, install ESP32 board support and upload it to the board. Pin assignments and timing values are defined in `Config.h`.
